protobuf==7.35.1
tornado==6.5.7
meshtastic==2.7.11
pypubsub==4.0.7
requests==2.34.2
Flask==3.1.3
twilio==9.10.9
unidecode==1.4.0
bleak==3.0.2
meshcore==2.3.8

# Optional: required ONLY for in-app ESP32 Meshtastic firmware flashing
# (firmware.allow_flashing=true). nRF52/UF2 + MeshCore use the web flasher and
# do not need this. Uncomment to enable on-device flashing:
# esptool>=4.7.0
